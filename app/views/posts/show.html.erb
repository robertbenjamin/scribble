<h2 class="post-title"><%= @post.title %> <%= link_to "(edit)", edit_post_path(@post) %></h2>
<h3>By: <%= @post.author %></h4>

<%= image_tag(@post.photo_url) %>

<p><%= @post.text %></p>

<!-- Comments go HERE -->

<h2>Add a Comment:</h2>
<%= render 'comments/form' %>

<h2>Comments:</h2>
<%= render @post.comments %>


<!-- @user - User.find_by(name: "Tom") -->
<!-- @post.comments.find_by(user_id : @user.id) -->
<!-- @post.comments.find_by(user_id: User.find_by(name: "Tom").id) -->